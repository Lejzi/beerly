import React from 'react';
import React, { useState, useEffect } from 'react';
const [list, setList] = useState(data);

const Cta = ({ data }) => {
	return (
		<div className='cta'>
			<p>Cta hello</p>
		</div>
	);
};

export default Cta;
